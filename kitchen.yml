---
driver:
  name: vagrant

network:
  - ["forwarded_port", { guest: 9000, host: 9000 }]

provisioner:
  name: chef_solo
  require_chef_omnibus: 12
  always_update_cookbooks: true

platforms:
  - name: ubuntu-18.04

suites:
  - name: default
    run_list:
      - recipe[ruby::default]
    attributes:
